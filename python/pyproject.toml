[project]
name = "rame"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["numpy>=1.24"]

[build-system]
requires = ["maturin>=1.14.1,<2"]
build-backend = "maturin"

[tool.maturin]
python-source = "."
module-name = "rame._native"
bindings = "pyo3"

[dependency-groups]
dev = []
